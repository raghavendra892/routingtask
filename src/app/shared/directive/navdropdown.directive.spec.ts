import { NavdropdownDirective } from './navdropdown.directive';

describe('NavdropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new NavdropdownDirective();
    expect(directive).toBeTruthy();
  });
});
